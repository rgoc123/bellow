<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>

  window.staticImages = {
    default_dish: "<%= image_url("default_dish.jpg") %>"
  }

</script>

<main id="root"></main>
